# Use the official lightweight Nginx image from Docker Hub
FROM nginx:alpine

# Copy all your frontend website files into the Nginx server directory
COPY . /usr/share/nginx/html

# Expose port 80 so we can access the web server
EXPOSE 80